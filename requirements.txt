# Runtime dependencies
fastapi
sqlmodel
sqlalchemy
pydantic
email-validator
python-jose
passlib
python-multipart
python-dotenv
# Include uvicorn extras for production-ready server features (watchfiles, httptools, etc.)
uvicorn[standard]
# Used by both tests and runtime (TMDb integration and TestClient)
httpx

# Test dependencies
pytest
